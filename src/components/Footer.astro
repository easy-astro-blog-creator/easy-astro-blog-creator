---
import { easyConfig } from '../siteConfig';
const today = new Date();
---

<aside class="footer-text">
	<p>
		Create your own GitHub Pages blog with <a class="" href="https://github.com/ShelbyJenkins/easy-astro-blog-creator"
			>easy-astro-blog-creator</a
		>!
	</p>
	<div></div>
	<p>&copy; {today.getFullYear()} {easyConfig.full_name}.</p>
</aside>

<style>
	.footer-text {
		display: flex;
		flex-direction: column;
		justify-content: flex-start;
		align-items: center;
		text-align: center;
		font-size: 0.5rem;
		margin-top: 1rem;
		margin-bottom: auto;
		margin-right: 1rem;
	}
	div {
		border-bottom-width: 1px;
		border-color: theme(colors.semantic.on-surface-variant);
		width: 12%;
		margin: auto;
		padding-top: 0.25rem;
	}
	a {
		background: linear-gradient(
			90deg,
			theme(colors.semantic.primary) 0%,
			theme(colors.semantic.secondary) 50%,
			theme(colors.semantic.tertiary) 100%
		);
		background-clip: text;
		-webkit-background-clip: text;
		color: transparent;
		font-weight: 600;
		filter: contrast(1) saturate(1) blur(0.1px);
		transition: 3s ease;
	}
	a:hover {
		filter: contrast(1.2) saturate(3) blur(0.2px);
	}
</style>
